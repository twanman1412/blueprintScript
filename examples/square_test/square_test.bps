blueprint isSquare {
    input: n: i32;
    output: bool;
    default: n == 0 ==> true;
    requires: n >= 0;
}

function bool isSquare(i32 n) {
    i32 odd = 1;
    while (n > 0) {
        n = n - odd;
        odd = odd + 2;
    }

    return n == 0;
}

print(isSquare(16));
